#!/bin/bash

dirs="blockchain-core/"

echo "Running stylish-haskell... fixing issues inline"
find $dirs -name "*.hs" | xargs stack exec -- stylish-haskell -i

echo "Running hlint..."
stack exec hlint $dirs
